{
  "name": "makpad",
  "version": "1.0.0",
  "description": "Realtime cloud notepad with ephemeral per-path attachments.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "minio": "^8.0.5",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
